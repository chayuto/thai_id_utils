AllCops:
  TargetRubyVersion: 2.7
  NewCops: enable
  Exclude:
    - 'vendor/**/*'
    - 'tmp/**/*'
    - '*.gemspec'
    - 'dataset/**/*'

# Frozen string literals required on all gem files
Style/FrozenStringLiteralComment:
  Enabled: true
  EnforcedStyle: always

# Large data hashes (CATEGORY_DESCRIPTIONS, PROVINCE_CODES) are legitimately long
Layout/LineLength:
  Max: 120

# Single-module gem with data constants (PROVINCE_CODES, DISTRICT_COUNTS, etc.)
# makes the module legitimately long
Metrics/ModuleLength:
  Max: 220

# generate() method has legitimate complexity from optional param handling
Metrics/MethodLength:
  Max: 25

Metrics/AbcSize:
  Max: 30

Metrics/CyclomaticComplexity:
  Max: 10

# Test classes grow naturally with coverage
Metrics/ClassLength:
  Exclude:
    - 'test/**/*'

# Module-level methods (self.method_name) are idiomatic for utility gems
Style/ModuleFunction:
  Enabled: false
